# Copy this file to `.env` and fill in your OpenAI API key.
# The backend reads it once at boot via src/env.ts and never exposes it to the browser.
OPENAI_API_KEY=sk-...
PORT=8787
DEFAULT_MODEL=gpt-5.4-mini
